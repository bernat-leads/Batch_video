"""Agent tools — placeholder for the template.

Add your application-specific tools here. Tools are functions that the agent
can call during conversation. Register them in the TOOLS list and wire them
into the graph via ToolNode.

Example:
    from langchain_core.tools import tool

    @tool
    def search_web(query: str) -> str:
        \"\"\"Search the web for information.\"\"\"
        return "Search results for: " + query

    TOOLS = [search_web]
"""

TOOLS: list = []
